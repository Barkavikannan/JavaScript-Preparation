//find second largest number

function secLarge(arr){
  let fmax=0;
  let smax=0;
  
  for(let i=0;i<arr.length;i++){
    if(a[i]>fmax){
      smax=fmax;
      fmax=a[i];
    }
  }
  
  return smax;
}

let a=[10,5,8,15,20];
console.log(secLarge(a));
