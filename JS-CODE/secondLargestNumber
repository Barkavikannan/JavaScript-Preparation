//find second largest number

function secLarge(arr){
  let fmax=0;
  let smax=0;
  
  for(let i=0;i<arr.length;i++){
    if(a[i]>fmax&&a[i]>smax){
      smax=fmax;
      fmax=a[i];
    }
    else if(a[i]>smax && a[i]!= fmax){
        smax=a[i];
    }
  }
  
  return smax;
}

let a=[55,10,5,8,15,20];
console.log(secLarge(a));
