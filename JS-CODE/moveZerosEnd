//move zeros to end(in-place)

function moveZeros (arr){
let lastIndex=arr.length-1

for(i=arr.length-1;i>=0;i--){
    if(arr[i]===0){
        let indexValue=arr[lastIndex]
        arr[lastIndex]=arr[i]
        arr[i]=indexValue
        
        lastIndex--
    }
}
return arr;
}

let a = [0, 1, 0, 3, 12];
console.log(moveZeros(a))



//2nd method by using array

let a=[0,1,0,3,0,0,12];
let result=[];
let LastIndex=a.length-1;

for(let i=0;i<a.length;i++){
    if(a[i]!=0){
        result.unshift(a[i]);
    }
    else{
        result.push(a[i]);
    }
}
console.log(result)
